background {
    monitor =
    path = screenshot   # only png supported for now

    # all these options are taken from hyprland, see https://wiki.hyprland.org/Configuring/Variables/#blur for explanations
    blur_passes = 2 # 0 disables blurring
    blur_size = 3
    noise = 0.007
    contrast = 0.8916
    brightness = 0.8172
    vibrancy = 0.3
    vibrancy_darkness = 0.1
}
input-field {
    monitor =
    size = 220, 50
    outline_thickness = 4
    dots_size = 0.33 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.15 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = false
    dots_rounding = -1 # -1 default circle, -2 follow input-field rounding
    outer_color = rgba(282a36ff)
    inner_color = rgba(282a36ff)
    font_color = rgba(e2e4e5ff)
    swap_font = true 
    placeholder_text = ... 
    fade_on_empty = true
    fade_timeout = 1000 # Milliseconds before fade_on_empty is triggered.
    hide_input = false
    rounding = -1 # -1 means complete rounding (circle/oval)
    check_color = rgba(9aedfeff)
    fail_color = rgba(ff5c57ff)
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
    fail_transition = 100 # transition time in ms between normal outer_color and fail_color
    capslock_color = rgba(f3f99dff)
    numlock_color = -1
    bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
    invert_numlock = false 

    position = 0, -20
    halign = center
    valign = center
}
